self-hosted-runner:
  # Custom label used by extended.yml's runs-on (see that file's own
  # status note -- it currently has no matching runner). actionlint only
  # knows GitHub's default hosted-runner labels out of the box; this
  # tells it "gpu-sm90" is a real, intentional label rather than a typo.
  labels:
    - gpu-sm90
